import { SubmitSingleInputSwapRequestBody } from '@raylac/shared';

const submitSingleInputSwapMock = async (
  _arg: SubmitSingleInputSwapRequestBody
): Promise<void> => {
  // Mock successful submission - no return value needed
};

export default submitSingleInputSwapMock;
