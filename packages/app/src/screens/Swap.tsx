import { View } from 'react-native';

const Swap = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        paddingHorizontal: 16,
      }}
    ></View>
  );
};

export default Swap;
